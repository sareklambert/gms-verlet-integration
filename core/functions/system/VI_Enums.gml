enum VI_TYPE {
	SYSTEM,
	OBJECT
};

enum VI_SUBTYPE {
	PHYSICAL,
	FORCEFIELD,
	COLLIDER,
	CONNECTOR
};

enum VI_PC_TYPE {
	POINT,
	INSTANCE,
	COLLIDER
};

enum VI_POINT_STATE {
	ALIVE,
	DELETED
};

enum VI_POINT_INDEX {
	FIRST,
	CENTER,
	LAST
};
